[build]
command = "npm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "24"

# This is the standalone Sanity Studio deployment
# Deployed as a separate Netlify site (not proxied from frontend)

