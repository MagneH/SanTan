[build]
command = "npm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "24"

# This is the standalone Sanity Studio deployment
# It will be proxied from the main frontend app at /studio

