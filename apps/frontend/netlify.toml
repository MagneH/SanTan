[build]
command = "npm run build"
publish = "dist/client"
functions = "netlify/functions"

[build.environment]
NODE_VERSION = "22"
# TanStack Start packages require >=22.12; Netlify will pick a 22.x fulfilling requirement.
