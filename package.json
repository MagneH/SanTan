{
  "name": "santan-monorepo",
  "private": true,
  "packageManager": "npm@11.3.0",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "echo '🚀 Starting development servers...' && turbo run dev",
    "dev:stream": "echo '📺 Starting development servers in stream mode...' && turbo run dev --ui=stream",
    "dev:tui": "echo '🖥️  Starting development servers in TUI mode...' && turbo run dev --ui=tui",
    "prebuild": "npm --workspace=packages/shared run build",
    "build": "echo '🏗️  Building all packages...' && turbo run build",
    "build:frontend": "npm --workspace=@santan/frontend run build",
    "build:cloudflare": "npm --workspace=@santan/frontend run build:cloudflare",
    "build:studio": "npm --workspace=@santan/shared run build && npm --workspace=@santan/studio run build",
    "lint": "echo '🔍 Linting code...' && turbo run lint",
    "type-check": "echo '✅ Type checking. ..' && turbo run type-check",
    "format": "echo '✨ Formatting code...' && turbo run format",
    "clean": "echo '🧹 Cleaning build artifacts...' && turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=24"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "*"
  }
}
